spring:
  application:
    name: movie-app
  jackson:
    deserialization:
      fail-on-unknown-properties: false

  config:
    import:
      - classpath:database.yml
      - classpath:movie-api.yml
      - optional:classpath:actuator.yml